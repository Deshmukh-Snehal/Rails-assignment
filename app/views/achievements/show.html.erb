<p id="notice"><%= notice %></p>

<p>
  <strong>Awards:</strong>
  <%= @achievement.awards %>
</p>

<p>
  <strong>Medals:</strong>
  <%= @achievement.medals %>
</p>

<p>
  <strong>Staus:</strong>
  <%= @achievement.status %>
</p>
<% if ((@achievement.user == current_user) || (current_user.admin?)) %>
  <%= link_to 'Edit', edit_achievement_path(@achievement), class:"btn btn-primary" %>
<% end %>  
<%= link_to 'Back', achievements_path, class:"btn btn-dark" %>