<%= form_with(model: sport,:html => {id: "playerValidate"}, local: true) do |form| %>
  <% if sport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sport.errors.count, "error") %> prohibited this sport from being saved:</h2>

      <ul>
      <% sport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :sport_name %>
    <%= form.text_field :sport_name, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :sport_equipments %>
    <%= form.text_field :sport_equipments, class:"form-control" %>
  </div>
  
  <div class="field form-group">
    <%= form.number_field :user_id,id: :sport_user_id, class:"form-control", value: current_user.id,type: :hidden %>
  </div>
   
  <br/>

  <div class="actions">
    <%= form.submit class:"btn btn-primary"%>
  </div>
<% end %>
