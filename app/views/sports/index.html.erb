<p id="notice"><%= notice %></p>


<div class="card">
  <div class="card-header">
    <h1>Sports</h1>
    <table class="table table-striped" id="server-side-sports">
    </table>
    <br>
    <% if current_user && current_user.admin? %>
    <%= link_to 'New Sport', new_sport_path,class:"btn btn-primary" %>
    <%end%>
  </div>
</div>